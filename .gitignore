# macOS
.DS_Store
**/.DS_Store

# Editor / tooling local config
.claude/settings.local.json
.vscode/
.idea/

# AI assistant per-session notes (CLAUDE.md 工作流约定：不作为主要记忆)
memory/

# Temp / cache
*.log
.cache/

# API 密钥配置（含 comfly / itlsj / MiniMax key，绝对不能提交）
js/config.js

# TTS 测试音频样本（生成时临时放置，commit 前清理）
tmp_tts_samples/
