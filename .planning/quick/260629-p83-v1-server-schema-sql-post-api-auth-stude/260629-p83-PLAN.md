---
phase: quick-260629-p83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/package-lock.json
  - server/schema.sql
  - server/README.md
  - server/src/routes/auth.js
  - server/src/middleware/auth.js
  - server/src/models/studentUserModel.js
  - server/src/services/passwordService.js
  - server/src/services/tokenService.js
  - server/src/controllers/studentAuthController.js
  - server/src/utils/httpError.js
autonomous: true
requirements:
  - QUICK-260629-P83
must_haves:
  truths:
    - "学生可以用手机号、密码、姓名、年级注册账号，密码只以 bcryptjs hash 形式进入数据访问层。"
    - "学生可以用手机号和密码登录，账号禁用或密码错误返回统一 JSON 错误。"
    - "已登录学生可以通过 GET /api/auth/me 获取自己的学生摘要，未登录或 token 无效会被拒绝。"
    - "本阶段不接前端、不实现同步 API、不部署生产、不要求 MySQL runtime 验证。"
  artifacts:
    - path: "server/src/models/studentUserModel.js"
      provides: "student_users 查询、创建、登录时间更新、设备记录数据访问层"
    - path: "server/src/services/passwordService.js"
      provides: "bcryptjs 密码 hash 与 verify 封装"
    - path: "server/src/services/tokenService.js"
      provides: "学生 JWT 签发与 payload 规范"
    - path: "server/src/controllers/studentAuthController.js"
      provides: "register/login/me 控制器"
    - path: "server/src/routes/auth.js"
      provides: "POST /api/auth/student/register、POST /api/auth/student/login、GET /api/auth/me 路由"
  key_links:
    - from: "server/src/routes/auth.js"
      to: "server/src/controllers/studentAuthController.js"
      via: "router.post/router.get handler binding"
      pattern: "student/(register|login)|/me"
    - from: "server/src/controllers/studentAuthController.js"
      to: "server/src/services/passwordService.js"
      via: "hashPassword / verifyPassword"
      pattern: "hashPassword|verifyPassword"
    - from: "server/src/controllers/studentAuthController.js"
      to: "server/src/models/studentUserModel.js"
      via: "create/find/update model calls"
      pattern: "studentUserModel"
    - from: "server/src/routes/auth.js"
      to: "server/src/middleware/auth.js"
      via: "requireAuth protects GET /api/auth/me"
      pattern: "requireAuth"
---

<objective>
Implement Backend Database V1 Stage 2 student authentication inside `server/` only.

Purpose: turn the Stage 1 auth placeholder into isolated student register/login/current-user endpoints that match the locked decisions: phone+password login, bcryptjs password hashing, no frontend integration, no sync API, no deployment, and no live MySQL verification requirement.

Output: student auth data access layer, password/token services, controller, routes, schema/package/docs updates, and syntax/keyword verification that can run without MySQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mac/Documents/写作demo/杨老师作文训练营/.planning/STATE.md
@/Users/mac/Documents/写作demo/杨老师作文训练营/CLAUDE.md
@/Users/mac/Documents/写作demo/杨老师作文训练营/docs/backend-database-v1-implementation-plan.md
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/README.md
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/package.json
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/schema.sql
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/app.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/routes/auth.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/middleware/auth.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/db.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/config/env.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/server/src/middleware/errorHandler.js
@/Users/mac/Documents/写作demo/杨老师作文训练营/.planning/quick/260629-p83-v1-server-schema-sql-post-api-auth-stude/260629-p83-CONTEXT.md

<locked_decisions>
- D-01: Student account uses phone + password. Registration accepts `phone`, `password`, `name`, `grade`; phone is unique login identifier. No SMS verification.
- D-02: Use `bcryptjs`; save only password hashes; centralize hash/verify outside controllers.
- D-03: Verification must not require live MySQL. Use `npm install`, `node --check`, dependency/route/model keyword checks.
- D-04: Keep scope isolated under `server/`; do not connect frontend, do not implement sync API, do not deploy production.
- D-05: Return existing unified JSON shape `{ code, message, data }`; protect `GET /api/auth/me` with existing JWT middleware.
</locked_decisions>

<interfaces>
Existing route mount:
```js
// server/src/routes/index.js
router.use('/auth', authRouter);
```
Therefore endpoints in this plan are:
- `POST /api/auth/student/register`
- `POST /api/auth/student/login`
- `GET /api/auth/me`

Existing auth middleware contract:
```js
function requireAuth(req, res, next) {
  // reads Authorization: Bearer <token>
  // sets req.auth = jwt.verify(token, env.jwtSecret)
}
```

Existing DB contract:
```js
function getPool() {
  // returns mysql2 promise pool when MYSQL_* is configured, otherwise null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add student data access and credential services</name>
  <files>server/package.json, server/package-lock.json, server/schema.sql, server/src/models/studentUserModel.js, server/src/services/passwordService.js, server/src/services/tokenService.js, server/src/utils/httpError.js</files>
  <behavior>
    - Password hashing uses bcryptjs only per D-02; controllers do not call bcryptjs directly.
    - `student_users.phone` remains unique and `student_users.password_hash` remains non-null per D-01/D-02; update schema only if these constraints are missing.
    - Data access uses `getPool()` and returns controlled service-unavailable errors when DB config/pool is unavailable, without requiring MySQL during syntax verification per D-03.
    - Token payload includes student identity and role/type sufficient for `GET /api/auth/me` authorization, without introducing frontend coupling per D-04.
  </behavior>
  <action>Run `cd /Users/mac/Documents/写作demo/杨老师作文训练营/server && npm install bcryptjs` to update `server/package.json` and `server/package-lock.json`. Create `passwordService.js` exporting `hashPassword(password)` and `verifyPassword(password, hash)` with a named salt round constant. Create `tokenService.js` exporting `signStudentAccessToken(student)` using `jsonwebtoken` and `env.jwtSecret`; fail with an exposed 500-style configuration error if `JWT_SECRET` is missing. Create `httpError.js` with a small helper that sets `statusCode`, `expose`, and optional `data` so existing `errorHandler` emits unified JSON. Create `studentUserModel.js` with functions for `findByPhone(phone)`, `findById(id)`, `createStudentUser({ phone, passwordHash, name, grade, stage })`, `updateLastLoginAt(id)`, and optional `upsertStudentDevice({ studentUserId, deviceId, localSchemaVersion })`. Use parameterized mysql2 queries only. Generate `student_uid` server-side with a stable prefix plus timestamp/random component; do not trust a client userId. Keep all files under `server/` per D-04.</action>
  <verify>
    <automated>cd /Users/mac/Documents/写作demo/杨老师作文训练营/server && npm install && node --check src/models/studentUserModel.js && node --check src/services/passwordService.js && node --check src/services/tokenService.js && node --check src/utils/httpError.js && grep -R "bcryptjs\|hashPassword\|verifyPassword" -n src/services src/controllers package.json && grep -n "UNIQUE KEY uk_student_users_phone\|password_hash" schema.sql</automated>
  </verify>
  <done>`bcryptjs` is installed, password/token/model utilities syntax-check, schema contains phone uniqueness and password_hash, and no new code outside `server/` is required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement student register, login, and me endpoints</name>
  <files>server/src/controllers/studentAuthController.js, server/src/routes/auth.js, server/src/middleware/auth.js</files>
  <behavior>
    - `POST /api/auth/student/register` validates phone/password/name/grade, hashes password, creates student, optionally records device metadata, signs token, returns `{ code: 0, message: 'ok', data: { accessToken, user } }` per D-01/D-02/D-05.
    - Duplicate phone returns HTTP 409 with unified JSON; invalid input returns 400; missing DB config returns controlled 503/500-style JSON rather than crashing per D-03.
    - `POST /api/auth/student/login` verifies phone+password, rejects wrong credentials with 401, rejects disabled status with 403, updates last_login_at/device if available, and returns token + user summary.
    - `GET /api/auth/me` is protected by `requireAuth`; it returns only the current student's summary and never accepts client-supplied userId per D-05.
  </behavior>
  <action>Replace the 501 catch-all in `server/src/routes/auth.js` with explicit student auth routes. Create `studentAuthController.js` with `registerStudent`, `loginStudent`, and `getCurrentStudent`. Validate phone as a trimmed 6-20 character account string suitable for Chinese mobile numbers without adding SMS verification; validate password length at least 6; validate grade integer 1-12 and derive stage as `S` for 1-6, `C` for 7-9, `H` for 10-12. Return normalized user fields using camelCase in API data: `id`, `studentUid`, `phone`, `name`, `grade`, `stage`, `abilityIndex`, `totalStars`, `status`, `lastLoginAt`, `createdAt`. Update `server/src/middleware/auth.js` only if needed so the JWT payload from `tokenService` exposes `req.auth.studentUserId` or equivalent for `GET /api/auth/me`; preserve existing `requireRole` behavior for future admin routes. Do not touch frontend files, sync routes, admin routes, deployment config, or production env files per D-04.</action>
  <verify>
    <automated>cd /Users/mac/Documents/写作demo/杨老师作文训练营/server && node --check src/controllers/studentAuthController.js && node --check src/routes/auth.js && node --check src/middleware/auth.js && grep -R "student/register\|student/login\|router.get('/me'\|requireAuth\|signStudentAccessToken\|verifyPassword" -n src/routes src/controllers src/middleware src/services</automated>
  </verify>
  <done>Auth routes no longer return the Stage 1 auth placeholder for student register/login/me, endpoint code has syntax-valid handlers, protected `me` uses JWT auth, and scope remains backend-only.</done>
</task>

<task type="auto">
  <name>Task 3: Update backend docs and aggregate verification</name>
  <files>server/README.md, server/package.json</files>
  <action>Update `server/README.md` from Stage 1 route status to Stage 2 auth status. Document the three active endpoints, request/response examples at a concise level, `bcryptjs` dependency, `JWT_SECRET` requirement for token issuing and `GET /api/auth/me`, and explicitly state this stage still does not connect frontend, does not implement sync API, does not deploy production, and does not require live MySQL verification per D-03/D-04. Update `server/package.json` `check` script so it includes every new JS file created in this plan and still avoids requiring a live MySQL server.</action>
  <verify>
    <automated>cd /Users/mac/Documents/写作demo/杨老师作文训练营/server && npm run check && grep -n "POST /api/auth/student/register\|POST /api/auth/student/login\|GET /api/auth/me\|bcryptjs\|no frontend\|MySQL" README.md && grep -n "studentAuthController\|studentUserModel\|passwordService\|tokenService\|httpError" package.json</automated>
  </verify>
  <done>`npm run check` covers all backend Stage 2 JS files without DB runtime, README accurately describes active auth endpoints and non-goals, and keyword checks prove the implementation surface is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→auth API | Untrusted registration/login JSON enters Express handlers. |
| auth API→database | Validated account fields and password hashes are written to MySQL. |
| client→JWT middleware | Bearer token controls access to current-user endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260629-p83-01 | S | `POST /api/auth/student/login` | mitigate | Verify bcryptjs hash server-side; return generic `invalid_phone_or_password` for credential mismatch. |
| T-quick-260629-p83-02 | T | `studentUserModel.js` SQL writes | mitigate | Use mysql2 parameterized queries; derive `student_uid` and `stage` server-side; ignore client-supplied user IDs. |
| T-quick-260629-p83-03 | I | `GET /api/auth/me` | mitigate | Protect route with `requireAuth`; use token subject/student id only; return student summary, never password_hash. |
| T-quick-260629-p83-04 | D | auth endpoints | accept | No live rate limiter in this isolated stage; keep input limits small via existing `express.json({ limit: '1mb' })` and document future rate limiting. |
| T-quick-260629-p83-05 | E | JWT payload/roles | mitigate | Use explicit student token fields and preserve `requireRole` for admin; do not grant admin role from student login. |
</threat_model>

<verification>
Run all verification without requiring a live MySQL server:

```bash
cd /Users/mac/Documents/写作demo/杨老师作文训练营/server
npm install
npm run check
grep -R "bcryptjs\|hashPassword\|verifyPassword" -n src package.json
grep -R "student/register\|student/login\|router.get('/me'\|requireAuth" -n src/routes src/controllers src/middleware
grep -n "UNIQUE KEY uk_student_users_phone\|password_hash" schema.sql
```

Do not run MySQL migrations, do not wire frontend, do not implement sync/admin functionality, and do not deploy.
</verification>

<success_criteria>
- `server/` contains a complete student auth Stage 2 implementation for register/login/me.
- Password handling is centralized through `bcryptjs`; password hashes are never returned in API responses.
- `GET /api/auth/me` depends on existing JWT middleware and current token identity.
- `server/schema.sql` has `student_users.phone` unique and `password_hash` non-null.
- `npm install`, `npm run check`, and keyword checks pass without requiring MySQL.
- No files outside `server/` and this planning directory are modified by execution.
</success_criteria>

<output>
After completion, create `/Users/mac/Documents/写作demo/杨老师作文训练营/.planning/quick/260629-p83-v1-server-schema-sql-post-api-auth-stude/260629-p83-SUMMARY.md`
</output>
