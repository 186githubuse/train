# Quick Task 260620-6f0: 新增模块首页(杨红作文6大模块壳)最小可用版 - Summary

**Completed:** 2026-06-19
**Code commit:** 391819e

## What changed

新增了最外层「模块首页」MVP，登录后先进入模块首页，再由「感觉训练」大卡进入原训练营。

### Implemented

- `views/moduleHome.js`
  - 新增 `renderModuleHome()` 视图。
  - 渲染学习概况状态栏、杨老师 IP 角位、6 个模块卡片。
  - 「感觉训练」为可点击进行中大卡，跳转 `trainingCamp`。
  - 思维 / 综合 / 审题 / 同步作文 / 三步法36讲为锁定卡，点击弹 Toast：`这个模块正在准备中，敬请期待 ✨`。

- `css/views/moduleHome.css`
  - 提炼 `mockup-layout.html` 左屏样式。
  - 使用 `mh-` 前缀避免污染其它视图。
  - 移动端 1 列，桌面 ≥1024px 3 列网格；感觉训练大卡跨两行，36讲横条跨两列。

- `js/router.js`
  - 注册 `moduleHome`。
  - 加入 `viewsWithHeader`。
  - 空历史 `goBack()` 回模块首页。

- `js/main.js`
  - 老用户默认落地页从 `trainingCamp` 改为 `moduleHome`。
  - 注释写明回退方法。

- `views/onboarding.js`
  - 注册/登录完成后从 `trainingCamp` 改为 `moduleHome`。
  - 按钮文案同步改为「进入模块首页」。

- `index.html`
  - 引入 `css/views/moduleHome.css?v=20260620a`。

## Verification

- CSS 括号计数：`css/views/moduleHome.css` 57/57，`css/style.css` 259/259。
- JS 语法：`node --check` 通过：
  - `views/moduleHome.js`
  - `js/router.js`
  - `js/main.js`
  - `views/onboarding.js`
- 静态资源 HTTP 检查：`index.html` / `moduleHome.css` / `moduleHome.js` / `router.js` / `main.js` 返回 200。
- Playwright + 系统 Chrome 真实交互烟测：
  - 老用户 localStorage 注入后打开 `http://localhost:8090/`，能看到 `.module-home-page`。
  - 「感觉训练」大卡可见。
  - 点击锁定「思维训练」显示包含「敬请期待」的 Toast。
  - 点击「感觉训练」进入 `trainingCamp`，`window.__router.getCurrentView()` 为 `trainingCamp`。
  - 截图输出：`/tmp/module-home-verify.png`。

## Notes

- 右屏专题选择页的面包屑 / 去前缀 / 配图改造未做，按任务范围留到下一步。
- 当前工作区在任务开始前已有未提交改动。本次代码提交使用选择性 staging，只提交模块首页相关改动；预存改动仍留在工作区。
- `mockup-layout.html` 是设计预览文件，未纳入代码提交。

## Rollback

快速回退入口：
1. `js/main.js`：把 `navigate('moduleHome', {}, false)` 改回 `navigate('trainingCamp', {}, false)`。
2. `views/onboarding.js`：把 `_enterApp()` 里的 `moduleHome` 改回 `trainingCamp`。
3. 若彻底移除模块首页，再删 `views/moduleHome.js`、`css/views/moduleHome.css`，并移除 `router.js` 注册和 `index.html` 样式 link。
