---
phase: quick-260620-6wm-topicdetail-phosphor-css-js
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - views/trainingCamp.js
  - views/topicDetail.js
  - css/views/topic.css
  - css/style.css
  - js/data/topics/index.js
autonomous: true
requirements:
  - QUICK-260620-6WM-TOPIC-ENTRY-UI
must_haves:
  truths:
    - "专题训练入口显示“感觉训练 › 选择专题”面包屑，用户一眼知道自己在感觉训练内选择专题。"
    - "静物/植物/动物/景物卡片不再重复出现“训练”字样，并展示专题图标 + 2~3 个对象预览。"
    - "人物/事件仍显示为锁定占位，不误导用户进入空页面。"
    - "专题入口与专题详情继续沿用现有马卡龙/液态玻璃视觉，不新增另一套配色。"
  artifacts:
    - path: "views/trainingCamp.js"
      provides: "专题入口卡片文案、预览结构、点击/锁定行为"
    - path: "views/topicDetail.js"
      provides: "动态专题名、引导文案，不再硬编码静物"
    - path: "css/views/topic.css"
      provides: "面包屑、预览缩略图、锁定占位、玻璃层样式"
    - path: "js/data/topics/index.js"
      provides: "专题显示名/预览数据最小补充，尤其景物预览"
  key_links:
    - from: "views/trainingCamp.js"
      to: "js/data/topics/index.js"
      via: "TOPICS map drives icon/title/preview rendering"
      pattern: "TOPICS\\.map|topic\\.(subs|previewItems)"
    - from: "views/topicDetail.js"
      to: "js/data/topics/index.js"
      via: "getTopic(topicId) provides dynamic display label"
      pattern: "getTopic\\(|replace\\(.*训练"
    - from: "css/views/topic.css"
      to: "views/trainingCamp.js / views/topicDetail.js"
      via: "breadcrumb and preview class names"
      pattern: "topic-breadcrumb|topic-preview"
---

<objective>
Refine the topic selection and topic-entry UI so category cards feel clearer and more premium without changing the learning flow.

Purpose: remove repetitive wording, expose visual cues for available topics, and keep locked topics honest.
Output: updated topic cards, breadcrumb, dynamic topic copy, and matching CSS for preview tiles.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@views/trainingCamp.js
@views/topicDetail.js
@css/views/topic.css
@css/style.css
@js/data/topics/index.js

<interfaces>
Current topic card inputs:
```js
topic = { id, title, subtitle, icon, colorClass, available, subs: [] }
```

Current hardcoded copy to replace:
```js
"选择静物开始答题"
"选择一个静物"
```

Use existing `sub.image` + `sub.title` for preview items where possible; keep `topic.colorClass` macaron gradients as the background source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize topic labels and preview metadata</name>
  <files>js/data/topics/index.js, views/trainingCamp.js, views/topicDetail.js</files>
  <action>Add a tiny topic display layer instead of renaming core data everywhere: derive a display label that strips the repeated `训练` suffix for card headlines and CTA copy, replace hardcoded “静物” strings in `topicDetail` with the current topic’s display label, and keep the full training flow unchanged. In `trainingCamp`, render breadcrumb text `感觉训练 › 选择专题` above the topic grid and map each available card to 2–3 preview objects using existing `topic.subs` first; if 景物 preview data is missing in the current branch, add minimal `previewItems` metadata in `js/data/topics/index.js` for 公园湖畔 / 状元桥 only. Keep 人物 / 事件 as locked placeholders and do not create empty routes.</action>
  <verify>
    <automated>node --check js/data/topics/index.js && node --check views/trainingCamp.js && node --check views/topicDetail.js && node --input-type=module - &lt;&lt;'NODE'
import { TOPICS } from './js/data/topics/index.js';
const previewIds = ['still-life', 'plant', 'animal', 'scenery'];
for (const id of previewIds) {
  const topic = TOPICS.find(t =&gt; t.id === id);
  if (!topic) throw new Error(`missing topic ${id}`);
  const previewCount = (topic.previewItems || topic.subs || []).length;
  if (previewCount &lt; 2) throw new Error(`topic ${id} missing preview items`);
}
for (const id of ['person', 'event']) {
  const topic = TOPICS.find(t =&gt; t.id === id);
  if (!topic || topic.available) throw new Error(`locked topic ${id} changed`);
}
console.log('topic preview metadata ok');
NODE</automated>
  </verify>
  <done>Topic cards and topicDetail copy use clean topic names, breadcrumb exists, preview inputs exist for 静物/植物/动物/景物, and 人物/事件 remain locked.</done>
</task>

<task type="auto">
  <name>Task 2: Build the breadcrumb and preview-card UI in existing macaron/glass style</name>
  <files>views/trainingCamp.js, css/views/topic.css, css/style.css</files>
  <action>Extend the existing topic-grid/card markup to add a breadcrumb row, icon badge, preview thumbnail strip or mini-collage, and short object labels for each available topic. Style everything in `css/views/topic.css` first, touching `css/style.css` only for the shared topic-grid/topic-card base rules that cannot be localized. Preserve the current `macaron-*` gradients and liquid-glass overlays; do not replace the palette or flatten cards into plain white tiles. Locked cards for 人物/事件 should keep the placeholder state but visually align with the upgraded layout.</action>
  <verify>
    <automated>python3 - &lt;&lt;'PY'
from pathlib import Path
css = Path('css/views/topic.css').read_text()
js = Path('views/trainingCamp.js').read_text()
for token in ['topic-breadcrumb', 'topic-preview', 'topic-preview-thumb', 'topic-card-icon-badge']:
    if token not in css and token not in js:
        raise SystemExit(f'missing token: {token}')
if css.count('{') != css.count('}'):
    raise SystemExit('unbalanced CSS braces in css/views/topic.css')
print('topic UI class hooks ok')
PY</automated>
  </verify>
  <done>Available topic cards show icon + object preview content, locked cards share the upgraded layout, and CSS stays within the existing macaron/liquid-glass system.</done>
</task>

<task type="auto">
  <name>Task 3: Keep topic-entry flow coherent after the visual upgrade</name>
  <files>views/topicDetail.js, css/views/topic.css</files>
  <action>Update `topicDetail` intro/list surfaces so the selected category reads naturally after the card rename: use the same cleaned display label in the header subtitle, intro CTA, and list hero text; remove hardcoded “选择静物开始答题” / “选择一个静物” copy; keep existing back navigation, video intro, and list-click behavior intact. Do not change quiz routing, scoring, or unrelated onboarding/trainingCamp logic.</action>
  <verify>
    <automated>python3 - &lt;&lt;'PY'
from pathlib import Path
text = Path('views/topicDetail.js').read_text()
for banned in ['选择静物开始答题', '选择一个静物']:
    if banned in text:
        raise SystemExit(f'hardcoded copy still present: {banned}')
if '感觉训练' not in Path('views/trainingCamp.js').read_text():
    raise SystemExit('breadcrumb text missing from trainingCamp.js')
print('topicDetail copy normalized')
PY</automated>
  </verify>
  <done>Topic entry copy is dynamic across categories, no static “静物” wording remains, and the existing topic navigation flow still points to the same routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| topic data → DOM | Topic titles, preview labels, and image URLs are interpolated into card markup. |
| locked card → navigation | Placeholder cards must not become clickable dead ends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260620-6wm-01 | Tampering | `views/trainingCamp.js` card rendering | mitigate | Keep preview content sourced from trusted local topic data and validate required preview inputs before rendering. |
| T-260620-6wm-02 | Denial of Service | locked topic entry flow | mitigate | Preserve `available` gating so 人物 / 事件 remain non-navigable placeholders. |
| T-260620-6wm-03 | Information Disclosure | preview image rendering | accept | This plan only reuses existing public COS image URLs already used by topic content; no new secret-bearing endpoints are introduced. |
</threat_model>

<verification>
Run the automated checks above, then optional smoke-test via local static server: open the training camp topic tab, confirm breadcrumb and preview cards, open at least one available topic, and confirm the intro/list copy uses the selected category name instead of a hardcoded “静物”.
</verification>

<success_criteria>
- 专题训练入口出现“感觉训练 › 选择专题”面包屑。
- 静物 / 植物 / 动物 / 景物卡片显示去重后的名称、Phosphor 图标、对象预览。
- 人物 / 事件仍是锁定占位，不进入空页面。
- `topicDetail` 不再写死静物文案，改为按当前专题动态显示。
- 现有马卡龙渐变和液态玻璃样式被延续，而不是改成新配色体系。
</success_criteria>

<output>
After completion, create `.planning/quick/260620-6wm-topicdetail-phosphor-css-js/260620-6wm-SUMMARY.md`
</output>
