---
phase: quick-260630-v5i-server-package-json-engines-node-18-16-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/README.md
autonomous: true
requirements:
  - QUICK-260630-V5I
---

# Quick Task 260630-v5i Plan

## Objective

Make the backend deployable on the current CentOS 7 ECS transition environment by documenting Node.js 16.20.2 via nvm and relaxing `server/package.json` from Node `>=18` to `>=16`.

## Tasks

1. Update `server/package.json` `engines.node` to `>=16`.
2. Update `server/README.md` requirements with the CentOS 7 / glibc 2.17 note, nvm Node 16.20.2 transition path, and long-term recommendation to migrate to a newer OS for Node 20+.
3. Verify package metadata and README contain the expected terms.

## Verification

```bash
grep -q '"node": ">=16"' server/package.json
grep -q 'Node.js 16.20.2 via nvm' server/README.md
grep -q 'glibc 2.17' server/README.md
```
