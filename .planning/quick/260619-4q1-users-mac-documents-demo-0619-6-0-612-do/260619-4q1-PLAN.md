---
phase: quick-260619-4q1-topic-bank-612
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/data/topics/index.js
  - js/data/topics/jingwu/taideng.js
  - js/data/topics/jingwu/bidai.js
  - js/data/topics/jingwu/shubao.js
  - js/data/topics/zhiwu/liushu.js
  - js/data/topics/zhiwu/mudan.js
  - js/data/topics/zhiwu/juzi.js
  - js/data/topics/dongwu/jumao.js
  - js/data/topics/dongwu/bixiong.js
  - js/data/topics/dongwu/yingwu.js
  - js/data/topics/dongwu/jinyu.js
  - js/data/topics/dongwu/mifeng.js
  - js/data/topics/jingwu/zhuangyuanqiao.js
  - js/data/topics/scenery/gongyuanhupan.js
  - views/topicQuiz.js
  - views/topicCompose.js
autonomous: true
requirements:
  - QUICK-260619-TOPIC-DATA-612
must_haves:
  truths:
    - "专题训练入口能显示最新 612 文档中的已开放对象；旧植物三项按最新题库替换，不沿用旧题。"
    - "每个对象从 A 类选择题进入 B 类单元训练，再进入 C 类综合书写；无图片的新对象仍能用占位图/占位描述正常打开。"
    - "所有题目可被浏览器 ES Module 正常导入，选择题字段完整，题号不重复，答案能匹配选项。"
    - "旧的静物/植物/动物对象仍能通过 topicQuiz 和 topicCompose 渲染，不因新模块化结构报错。"
  artifacts:
    - path: "js/data/topics/index.js"
      provides: "专题模块开放状态、子内容导入与对象列表"
    - path: "js/data/topics/**/*.js"
      provides: "按 612 DOCX 转换后的专题题库数据"
    - path: "views/topicQuiz.js"
      provides: "A/B/C 题目队列兼容模块化 typeA/typeB/typeC 数据"
    - path: "views/topicCompose.js"
      provides: "C 类/综合书写树形图与评分入口兼容模块化数据"
  key_links:
    - from: "js/data/topics/index.js"
      to: "js/data/topics/*/*.js"
      via: "ES module imports and TOPICS[].subs"
      pattern: "import .* from './(jingwu|zhiwu|dongwu|scenery)/.*\\.js'"
    - from: "views/topicQuiz.js"
      to: "moduleized topic data"
      via: "queue adapter flattens typeA.questions and typeB.units[].questions"
      pattern: "typeB.*units|questions"
    - from: "views/topicCompose.js"
      to: "typeC.totalTreeMap / essay or legacy typeD"
      via: "task adapter selects moduleized essay first, legacy typeD fallback"
      pattern: "totalTreeMap|typeD"
---

<objective>
根据 `/Users/mac/Documents/写作demo/0619最新/感觉训练专题模块开发大纲（新6.0简版）612.docx` 的提取文本，梳理影响范围后，将专题训练题库替换/补齐为最新模块化题库数据。

Purpose: 用户明确要求先梳理影响范围和可验证成功标准，不直接大规模改动；本计划把工作拆成“影响清单 → 数据转换/补齐 → 自动校验与最小视图兼容”三步。

Output: 最新专题题库数据模块、必要的 index 导入/开放状态、必要的 topicQuiz/topicCompose 模块化兼容适配，以及可重复运行的数据校验命令。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@js/data/topics/index.js
@js/data/topics/jingwu/taideng.js
@js/data/topics/jingwu/bidai.js
@js/data/topics/jingwu/shubao.js
@js/data/topics/zhiwu/liushu.js
@js/data/topics/zhiwu/mudan.js
@js/data/topics/zhiwu/juzi.js
@js/data/topics/dongwu/jumao.js
@js/data/topics/dongwu/bixiong.js
@js/data/topics/dongwu/yingwu.js
@js/data/topics/dongwu/jinyu.js
@views/topicQuiz.js
@views/topicCompose.js
@/Users/mac/.claude/projects/-Users-mac-Documents---demo---------/88c1da8b-36b2-46d2-8f65-1d812e7dff8c/tool-results/bivtofeor.txt

<interfaces>
Current topic module shape observed in existing files:
```js
export const TAIDENG = {
  id: 'taideng', topicId: 'still-life', title: '台灯', subtitle: '...',
  difficulty: 2,
  image: 'https://...', imageAlt: '...',
  typeA: [{ id, type, dim, text, options, correct, hint }],
  typeB: [{ id, type, dim, text, options, correct, hint }],
  typeC: [{ id, type, dim, text, options, correct, hint }],
  typeD: { id, title, requirements, treeMap: { root, branches: [{ name, color, children: [{ name }] }] }, sample }
}
```

Latest modular shape from project memory / 612 requirements that may need renderer compatibility:
```js
{
  schema: 'unit',
  typeA: { questions: [], write: { id, prompt, requirement, points, reference, treeMap } },
  typeB: { units: [{ id, title, questions: [], treeMap: { title, nodes: [] }, write: { id, prompt, requirement, points, reference } }] },
  typeC: { totalTreeMap: { title, units: [] }, essay: { id, title, requirements, reference } }
}
```

Relevant current renderer contracts:
```js
// views/topicQuiz.js currently builds queue as:
const arrA = _sub.typeA || [];
const arrB = _sub.typeB || [];
const arrC = _sub.typeC || [];
_queue = [...arrA, ...arrB, ...arrC];

// views/topicCompose.js currently expects:
_task = _sub?.typeD;
_task.treeMap.branches.map(...);
```
If these adapters are not already present in the working tree at execution time, add them before converting data to moduleized schema.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce an impact map before editing data</name>
  <files>none (read-only impact analysis before code edits)</files>
  <action>Before modifying files, read the DOCX extraction at `/Users/mac/.claude/projects/-Users-mac-Documents---demo---------/88c1da8b-36b2-46d2-8f65-1d812e7dff8c/tool-results/bivtofeor.txt` and the current `js/data/topics/` tree. Build an in-session impact map listing: source object headings, target module file, topicId, whether it replaces an existing object or creates a new one, whether image URLs exist, and whether a placeholder image/alt is needed. Use the document headings already present in the extracted text: 台灯、笔袋、书包、柳树、白牡丹、橘子、橘猫、比熊犬、虎皮鹦鹉、金鱼、蜜蜂、文瀛湖状元桥、公园湖畔. Treat the three plant files as replacements from latest source, not as already-complete. Do not skip objects just because images are missing; assign a deterministic placeholder image strategy using either an existing category placeholder URL if present or the closest existing category image with clear `imageAlt` marking it as 临时占位.</action>
  <verify>
    <automated>python3 - <<'PY'\nfrom pathlib import Path\np=Path('/Users/mac/.claude/projects/-Users-mac-Documents---demo---------/88c1da8b-36b2-46d2-8f65-1d812e7dff8c/tool-results/bivtofeor.txt')\ntext=p.read_text(errors='ignore')\nfor name in ['台灯','笔袋','书包','柳树','白牡丹','橘子','橘猫','比熊','虎皮鹦鹉','金鱼','蜜蜂','状元桥','公园湖畔']:\n    assert name in text, f'missing source heading/content for {name}'\nprint('source headings present')\nPY</automated>
  </verify>
  <done>Executor has a concrete object-to-file mapping and knows which files are replacements vs new additions before any large data edit begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace and add topic data modules from the latest 612 source</name>
  <files>js/data/topics/index.js, js/data/topics/jingwu/taideng.js, js/data/topics/jingwu/bidai.js, js/data/topics/jingwu/shubao.js, js/data/topics/zhiwu/liushu.js, js/data/topics/zhiwu/mudan.js, js/data/topics/zhiwu/juzi.js, js/data/topics/dongwu/jumao.js, js/data/topics/dongwu/bixiong.js, js/data/topics/dongwu/yingwu.js, js/data/topics/dongwu/jinyu.js, js/data/topics/dongwu/mifeng.js, js/data/topics/jingwu/zhuangyuanqiao.js, js/data/topics/scenery/gongyuanhupan.js</files>
  <behavior>
    - Importing `js/data/topics/index.js` succeeds in Node ES module mode with no syntax errors.
    - Every available sub has `id`, `topicId`, `title`, `image`, `imageAlt`, and a non-empty A/B/C training path.
    - For choice questions, every `correct` answer is present in `options`/`items`; multi-answer arrays all match option keys.
    - The three plant modules `liushu`, `mudan`, `juzi` contain the latest extracted 612 source questions, not stale old text.
    - New objects without delivered image assets use placeholders and descriptive alt text; no broken empty `image` strings.
  </behavior>
  <action>Convert the latest source content into browser-compatible ES modules matching the existing style. Prefer the moduleized schema (`schema:'unit'`, `typeA.questions`, `typeB.units[]`, `typeC.totalTreeMap/essay`) when the source has unit tree maps and per-unit writing tasks. If a current view adapter still only supports legacy arrays, keep enough backwards-compatible fields or complete Task 3 adapter work before relying on the new shape. Update `index.js` imports and `TOPICS` so replacements appear under their current categories; add 蜜蜂 under animal training unless an explicit insect top-level already exists; add 状元桥 and 公园湖畔 under scenery and set scenery `available: true`. Do not add unrelated product modules, backend, or image-generation work. Preserve vanilla JS: no package.json, no build step, no external dependencies.</action>
  <verify>
    <automated>node --input-type=module - <<'NODE'\nimport { TOPICS, getSub } from './js/data/topics/index.js';\nconst available = TOPICS.filter(t => t.available);\nif (!available.length) throw new Error('no available topics');\nconst subs = available.flatMap(t => (t.subs || []).map(s => ({ topic: t, sub: s })));\nconst required = ['taideng','bidai','shubao','liushu','mudan','juzi','jumao','bixiong','yingwu','jinyu','mifeng','zhuangyuanqiao','gongyuanhupan'];\nfor (const id of required) {\n  if (!subs.some(x => x.sub.id === id)) throw new Error(`missing sub ${id}`);\n}\nfunction questionsOf(sub) {\n  const a = Array.isArray(sub.typeA) ? sub.typeA : (sub.typeA?.questions || []);\n  const b = Array.isArray(sub.typeB) ? sub.typeB : (sub.typeB?.units || []).flatMap(u => u.questions || []);\n  const c = Array.isArray(sub.typeC) ? sub.typeC : (sub.typeC?.questions || []);\n  return [...a, ...b, ...c];\n}\nfor (const { topic, sub } of subs) {\n  if (getSub(topic.id, sub.id) !== sub) throw new Error(`getSub failed ${topic.id}/${sub.id}`);\n  for (const k of ['id','topicId','title','image','imageAlt']) if (!sub[k]) throw new Error(`${sub.id} missing ${k}`);\n  const qs = questionsOf(sub);\n  if (!qs.length) throw new Error(`${sub.id} has no choice questions`);\n  const seen = new Set();\n  for (const q of qs) {\n    if (!q.id || seen.has(q.id)) throw new Error(`${sub.id} duplicate/missing qid ${q.id}`);\n    seen.add(q.id);\n    if (q.type === 'sort') {\n      if (!q.items || !Array.isArray(q.correct)) throw new Error(`${q.id} invalid sort`);\n      for (const ans of q.correct) if (!(ans in q.items)) throw new Error(`${q.id} bad sort answer ${ans}`);\n    } else {\n      if (!q.options || !q.correct) throw new Error(`${q.id} missing options/correct`);\n      const answers = Array.isArray(q.correct) ? q.correct : [q.correct];\n      for (const ans of answers) if (!(ans in q.options)) throw new Error(`${q.id} answer ${ans} not in options`);\n    }\n  }\n}\nconsole.log(`topic data ok: ${subs.length} subs`);\nNODE</automated>
  </verify>
  <done>Latest source objects are represented in data modules, index wiring exposes them, and automated import/schema checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the smallest necessary view compatibility and validation checks</name>
  <files>views/topicQuiz.js, views/topicCompose.js</files>
  <behavior>
    - `topicQuiz` can build a quiz queue from both legacy arrays and moduleized `{ questions, units }` data.
    - `topicCompose` can render a writing task from either legacy `typeD` or moduleized `typeC.essay + typeC.totalTreeMap` without crashing.
    - The old objects and newly added objects can be opened by `getSub(topicId, subId)` and have a valid writing task.
  </behavior>
  <action>If the current view files already support the 611/612 moduleized schema, only regression-check and avoid unnecessary edits. If not, add small adapter helpers rather than rewriting the views: in `topicQuiz.js`, normalize A/B/C questions with functions such as `getTypeAQuestions(sub)`, `getTypeBQuestions(sub)`, `getTypeCQuestions(sub)` so `_queue` stays an array of choice/sort questions. In `topicCompose.js`, normalize the writing task so legacy `typeD` still works and moduleized data maps `typeC.essay` plus `typeC.totalTreeMap` into the existing render contract; update tree rendering only as much as needed to support `branches` or `units/nodes`. Do not change AI provider config, OCR behavior, score thresholds, TTS, or CSS unless a rendering bug is directly caused by the schema adapter.</action>
  <verify>
    <automated>node --check views/topicQuiz.js && node --check views/topicCompose.js && node --input-type=module - <<'NODE'\nimport { TOPICS } from './js/data/topics/index.js';\nfunction questionsOf(sub) {\n  const a = Array.isArray(sub.typeA) ? sub.typeA : (sub.typeA?.questions || []);\n  const b = Array.isArray(sub.typeB) ? sub.typeB : (sub.typeB?.units || []).flatMap(u => u.questions || []);\n  const c = Array.isArray(sub.typeC) ? sub.typeC : (sub.typeC?.questions || []);\n  return { a, b, c, all: [...a, ...b, ...c] };\n}\nfunction hasWritingTask(sub) {\n  return Boolean(sub.typeD || sub.typeC?.essay || sub.typeA?.write || (sub.typeB?.units || []).some(u => u.write));\n}\nfor (const topic of TOPICS.filter(t => t.available)) {\n  for (const sub of topic.subs || []) {\n    const q = questionsOf(sub);\n    if (!q.all.length) throw new Error(`${topic.id}/${sub.id} no quiz queue`);\n    if (!hasWritingTask(sub)) throw new Error(`${topic.id}/${sub.id} no writing task`);\n  }\n}\nconsole.log('view adapter preconditions ok');\nNODE</automated>
  </verify>
  <done>Views can consume the updated data shape without syntax/runtime-precondition failures, while preserving old behavior and avoiding unrelated UI changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOCX extraction → JS modules | Source text is manually converted into executable JavaScript data; malformed text could become syntax errors or broken data. |
| Data modules → DOM rendering | Question text, hints, and samples are interpolated into `innerHTML`; content is trusted curriculum text but must not introduce accidental scripts. |
| Browser → AI grading API | Existing writing submission sends student text to configured AI API; this plan does not change provider or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260619-01 | Tampering | `js/data/topics/**/*.js` | mitigate | Run ES module import/schema checks to catch malformed IDs, answers, and missing fields before manual testing. |
| T-260619-02 | Information Disclosure | `views/topicCompose.js` / `js/config.js` | accept | This plan does not alter existing AI key exposure; backend proxy remains a separate launch-hardening task already noted in project memory. |
| T-260619-03 | Denial of Service | `views/topicQuiz.js` queue generation | mitigate | Adapter must return finite arrays and handle missing optional fields with fallbacks so one malformed sub does not crash all topic navigation. |
| T-260619-04 | Elevation of Privilege | Topic content rendered through `innerHTML` | accept | Source is local trusted curriculum content; do not add user-provided HTML rendering paths in this task. |
</threat_model>

<verification>
Run all task-level automated checks from the repository root. Then optionally start a local HTTP server with `python3 -m http.server 8080` and manually smoke-test: 训练营 → 专题训练 → 静物/植物/动物/景物 → open at least one replacement object and one new placeholder-image object → answer one A question → navigate back successfully.
</verification>

<success_criteria>
- Impact range is clear before large edits: exact objects, files, replacements, additions, and placeholder image needs are known.
- Latest 612 source replaces existing static/plant/animal modules where applicable; plants are not treated as already done.
- New source objects are wired into `TOPICS` with placeholders when image assets are missing.
- `node --input-type=module` import/schema validation passes for all available topics/subs.
- `node --check views/topicQuiz.js` and `node --check views/topicCompose.js` pass.
- No framework, build system, package manager, backend, or unrelated style refactor is introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260619-4q1-users-mac-documents-demo-0619-6-0-612-do/260619-4q1-SUMMARY.md` if the active GSD executor workflow requires summaries; otherwise report the completed scope and verification commands in the final response.
</output>
